{
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "gray-matter": "latest",
    "tree-sitter": "latest",
    "tree-sitter-typescript": "latest",
    "typescript": "latest"
  },
  "license": "Apache-2.0",
  "name": "agent-infrastructure",
  "private": true,
  "scripts": {
    "test": "bun run test:lint & lint_pid=$!; bun run test:unit & unit_pid=$!; bun run test:type & type_pid=$!; wait $lint_pid; lint_status=$?; wait $unit_pid; unit_status=$?; wait $type_pid; type_status=$?; [ $lint_status -eq 0 ] && [ $unit_status -eq 0 ] && [ $type_status -eq 0 ]",
    "test:lint": "bun scripts/lint.ts",
    "test:type": "tsc",
    "test:unit": "bun test ."
  },
  "type": "module",
  "version": "0.0.0"
}
