# Workspace Detection

Inspect the selected workspace for project root, source layout, stack, tests,
and browser/runtime boundaries. Evidence names inspected files and the
greenfield/brownfield classification.
